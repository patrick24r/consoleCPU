{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo flash bootloader",
            "type": "shell",
            "command": "cargo",
            "args": ["make", "bootloader"],
            "problemMatcher": [
                "$rustc"
            ],
            "group": "build"
        }
    ]
}